#include <stdio.h>
#include <stdlib.h>
#include <unistd.h> 

int main(int argc, char **argv) 
{
	if (argc == 3) {
		printf("That`s my first arg: %s\nThat`s my work time arg: %s\n", argv[1],argv[2]);
	}
	sleep(atoi(argv[2]));
return 0;
}